Edit email-notifications for Manufacturers
    and:
    adding uuid field for manufacturers (imagepath);
    domain confirmation at yandex email;
    clean conig/mail.php and .env(!) files: remove 
        config("mail.mail_to_test"), 
        config("mail.name_info"), 
        config("mail.mail_info");
    adding field uniqueness check when updating.
